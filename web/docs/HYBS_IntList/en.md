# Int List

Creates a LIST of editable integer values. It starts with one value, `1`.

Use it for integer parameters in workflows that repeat or iterate over a list.

## Outputs

- **int list**: Integer values.
- **count**: Number of entries.
