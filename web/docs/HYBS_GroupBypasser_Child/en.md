# Group Bypass Child

Frontend-only marker for a group-bypass cascade target.

Connect **from_parent** from Group Bypass Parent or another Child. Connect **to_children** to another Child to build multi-level cascades. The backend passes its input through unchanged.
