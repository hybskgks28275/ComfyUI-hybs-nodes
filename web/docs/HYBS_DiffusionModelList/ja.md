# Diffusion Model List

`models/unet` の UNet / diffusion model を選び、ファイル名の LIST を作成します。

最初は選択欄が 1 つです。モデルを選ぶと次の選択欄が追加されます。`count` は実際に選択したモデルだけを数えます。必要に応じて ComfyLab XY Plot のラベルに `model names` を使用できます。

## 出力

- **model names**: 表示行の順序で並んだ選択ファイル名。
- **count**: 選択モデル数。
