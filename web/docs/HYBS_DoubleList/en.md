# Double List

Creates a LIST of editable floating-point values. It starts with one value, `1.0`.

Use it as a separate strength list for LoRA List or wherever a workflow needs a list of decimal parameters.

## Outputs

- **double list**: Floating-point values.
- **count**: Number of entries.
