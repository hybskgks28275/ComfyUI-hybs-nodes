# Group Bypass Parent

Frontend-only marker for the source of a group-bypass cascade.

Connect **to_children** to a Group Bypass Child **from_parent** input. Toggling the parent group from Group Bypasser then also toggles linked child groups. The backend performs no work.
