# Group Bypasser

メイングラフとサブグラフのグループを一覧表示し、bypass 状態を切り替えるフロントエンド専用パネルです。

グループの順序は auto / custom で管理できます。custom ではドラッグ＆ドロップによる並べ替えが可能です。Group Bypass Parent と Group Bypass Child を接続すると、親グループの切り替えを関連する子グループへカスケードできます。バックエンド処理は行いません。
